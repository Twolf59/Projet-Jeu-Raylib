#ifndef MAIN_H
#define MAIN_H

//manage scene 
void manager();

//verify if the current scene is active
bool isActive(int scene);

//set the new active scene
void setScene_active();

#endif